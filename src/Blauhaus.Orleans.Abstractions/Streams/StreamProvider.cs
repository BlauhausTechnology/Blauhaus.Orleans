namespace Blauhaus.Orleans.Abstractions.Streams
{
    public static class StreamProvider
    {
        public const string Transient = nameof(Transient);
        public const string Persistent = nameof(Persistent);
    }
}